{
  "name": "memolife",
  "version": "1.0.0",
  "description": "MemoLife - AI Personal Assistant",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "cd frontend && npx expo start",
    "dev": "cd frontend && npx expo start",
    "build": "cd frontend && npx expo build",
    "backend": "cd backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "private": true
}
